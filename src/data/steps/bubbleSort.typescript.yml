version: "1"
language: typescript
algorithm: bubble-sort

steps:
  - lineStart: 1
    note: |
      This function initializes the sorting process.
      It defines the signature and sets up the main parameters.

      Notice how the algorithm uses an in-place strategy.

  - lineStart: 2
    lineEnd: 12
    note: Outer and inner loops

  - lineStart: 6
    lineEnd: 10
    note: Comparison and swap